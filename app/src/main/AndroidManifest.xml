<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.fantasycricket">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NotificationsActivity"></activity>
        <activity android:name=".FAQActivity" />
        <activity android:name=".AdminUserDetailsActivity" />
        <activity android:name=".AdminUserListActivity" />
        <activity android:name=".ChooseCaptainActivity" />
        <activity android:name=".LeaderboardActivity" />
        <activity android:name=".CurrentPointsActivity" />
        <activity android:name=".MyContestsActivity" />
        <activity android:name=".MyMatchesActivity" />
        <activity android:name=".AdminPanel" />
        <activity android:name=".MyAccountActivity" />
        <activity android:name=".CreateTeamActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContestActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity
            android:name="com.paytm.pgsdk.PaytmPGActivity"
            android:configChanges="keyboardHidden|orientation|keyboard"
            android:screenOrientation="portrait" />
        <activity android:name=".checksum" />
    </application>

</manifest>